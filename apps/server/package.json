{
  "name": "apps-server",
  "version": "1.0.0",
  "private": true,
  "description": "PictureStoreKeeper for server",
  "main": "index.js",
  "scripts": {
    "build": "pnpm build:script && node ./scripts/dist/build.js",
    "build:script": "esbuild ./scripts/build.ts --bundle --outfile=./scripts/dist/build.js --minify --platform=node --external:fs-extra"
  },
  "author": "Amigo (WhiteLuckBringers)",
  "license": "MPL-2.0",
  "dependencies": {
    "core-ui": "workspace:*"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.6",
    "esbuild": "^0.8.32",
    "fs-extra": "^9.0.1"
  }
}
